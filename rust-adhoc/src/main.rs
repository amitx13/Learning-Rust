//Wap to take a num as a Input and return True and False if num is even or odd

use std::io;

fn main(){
    println!("Enter to number to check if it is even or not");
    let mut num = String::from("");
}